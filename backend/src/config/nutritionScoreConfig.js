/**
 * Nutrition Score Configuration
 * Configuration settings for the nutrition score feature
 */

const config = {
    /**
     * Configuration for ratio-based nutrition score calculation
     */
    showDetailedAnalysis: true,

    /**
     * Whether to store detailed ratio analysis in the database
     */
    storeDetailedAnalysis: true
};

module.exports = config;
